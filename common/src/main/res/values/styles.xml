<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
    </style>
    <!-- 基本主题-->
    <style name="BaseAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">#dd000000</item>
        <item name="colorPrimaryDark">#dd000000</item>
        <item name="colorAccent">#dd000000</item>
        <item name="android:statusBarColor">#dd000000</item>
    </style>

    <!--沉浸式状态栏主题-->
    <style name="BaseAppThemeTranslucentBars" parent="BaseAppTheme">
        <!-- <item name="android:windowIsTranslucent">true</item>-->
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <!--Android 5.x开始需要把颜色设置透明，否则导航栏会呈现系统默认的浅灰色-->
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- 首页tab新样式 -->
    <style name="home_tab_style">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">5dp</item>
        <item name="android:layout_weight">1.0</item>
        <item name="android:orientation">vertical</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
    </style>

    <!-- 首页tab的文字样式 -->
    <style name="home_tab_tv_style">
        <item name="android:textSize">10sp</item>
        <item name="android:textColor">#999999</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:duplicateParentState">true</item>
    </style>


</resources>
